# P1/P2候选验收清单

署名：Laiqh

状态：DRAFT。用于审查隔离机器契约候选，不是共同批准记录。

依据：../runtime-core/review-response.md §留给架构侧的最小输入、§修订时额外消除的歧义。调度接口权威候选仍为../runtime-core/interfaces.md，禁止另起竞争调度协议。

## P1必须交付

- 精确引用类型、命名空间、版本、摘要及规范化约定。区分定义/对象/证据，文件位置不等于授权。
- 运行投影与节点依赖、执行类别、规则/验收Provider映射；检查缺依赖、循环和不匹配。
- 对应调度接口字段的映射与错误出口；业务模型不复制入核心。
- 缺失、未知版本、摘要不符、未知Provider必须用可执行反例验证。

## P2必须交付

- 准入、撤销、消费权及迁移批准的权威引用和核验责任；核心不签发。
- 持久instance、deployment、短期worker session分开，幂等动作身份不因attempt重试变化。
- 复制配置不得自动获得消费权；授权代际和有效期必须参与核验。
- 不可信撤销拒收；可信来源同步缺口在声明范围内阻断，不能伪造事件暂停任意业务。
- 合成可信控制面与用户提交载荷隔离。传入approved=true、自报摘要或本地文件存在不能充当可信批准。

## 验证与冻结

1. 保存真实失败→修正→通过输出，区分结构Schema与跨字段/可信状态语义校验。
2. 独立回读产物，检查所有通过测试的数目；子代理自报不直接当验收。
3. 权威服务、生产凭据、真实撤销同步和跨机唯一消费没有实现就明确标未验证。
4. 将候选Schema与核心§6逐字段对照。双方接受记录须绑定精确版本/摘要；本清单不签发批准。
5. 未共同冻结前，禁止以本地契约测试通过为由写运行核心。
