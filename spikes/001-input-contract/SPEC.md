# 输入契约可执行验证
署名：Laiqh
状态：隔离SPIKE；外部任务系统未配置。用户“那就开始做”确认上一轮的来源替换验证边界，不等于正式接口已双方冻结。

## 问题与方案
验证一份机器模型能约束Excel和SQLite不同来源，同一确定性处理器不随来源改动。模型定义语义及校验，接头仅映射列；对象实例目录与模型分离。输出标准快照及判定，非外部业务动作。

## 范围和验收
- 实际读取合成xlsx和只读SQLite，得到等价的标准事实与判定，保留不同来源。
- 缺字段、未知对象、重复对象、类型/单位错误、日期无时区、过期或未来时间拒绝整批输入，不交给处理器。
- 时间精度仅支持整秒或秒后1–6位小数（微秒）；观测时间与CLI的--as-of均适用。超过6位（包括多余零）以INVALID_TIME拒绝，不截断或舍入；合法精度内的未来/过期规则不变。
- 本SPIKE仅支持record_schema.properties的直接属性描述符为对象；布尔描述符虽是合法JSON Schema形式，但不在本片段支持子集内，须在读取来源前以MODEL_INVALID拒绝。对象描述符仍按既有Schema规则验证，不扩展任意模型能力。
- 绑定模型版本/摘要、对象目录摘要及带明确范围的来源摘要，保存不可覆写快照。Excel绑定本次读取的文件字节（file_bytes）；SQLite绑定本次所选行的规范化JSON（selected_rows），不声称覆盖整个数据库文件或其余表。版本不兼容或快照损坏拒绝。
- 一个合成读数判定夹具：DEMO-ASSET-A读数5为WITHIN_LIMIT，DEMO-ASSET-B读数15为OVER_LIMIT；阈值10只用于测试，不代表任何生产规则。
- 真实入口：python demo.py --output-dir <新的输出目录>；也提供cli.py单次读取入口。
- Required level：L1契约＋L2文件、SQLite、子进程/重复快照；L3用户亲手体验未验证，L4不适用。无真实凭据、现场输入或人工授权动作。
- 前端检测：本片段仅CLI与JSON报告，无前端，不建设看板。
- 无外部写入；所有生成文件仅在明确输出目录，已有文件不覆盖。临时测试夹具由TemporaryDirectory清理，演示证据保留。

## 方案拷问结论
用户已确认隔离最小链路与异常拦截；语义只维护一份，事实存储不成为第二模型；暂不做实时回源，过期明确拒绝。xlsx采用本机已存在openpyxl，模型校验采用已存在jsonschema；不安装软件。单机环境通过不升级为自包含发行验收。

## 非目标
调度服务、真实接头、AI推理、规则平台、包安装器、权限认证/签名、多租户、外部回读和完整图谱均不在本片段。映射是待整体冻结的可执行候选。
